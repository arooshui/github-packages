# push a image to github package (GHCR)
1. create a image
2. create a PAT on github
3. authenticate ghcr
4. tag and push our image to ghcr

# use github actions to publish a docker image to github package (GHCR)